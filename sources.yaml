# sources.yaml — validated entry points for Routine 1 (Collect)
#
# Each source is a stable blog INDEX / announcements page. Routine 1 starts here
# and follows the fresh links; it never relies on hard-coded article URLs.
#
# Fields:
#   key         Stable identifier. MUST match the key used in data/state.json.
#   platform    google_ads | meta | tiktok | linkedin | bing
#   title       Human-readable source name (goes into updates.json -> "source").
#   url         Entry point (index / announcements page).
#   type        official | aggregator
#                 official   = the platform's own blog. Preferred canonical record.
#                 aggregator = 3rd-party (Search Engine Land). Often repeats official
#                              stories -> resolved by dedup Layer 2 (see criteria.md).
#   validation  readable | thin | needs-browser | broken | pending
#                 pending        = not yet fetch-tested (do it in an env with egress
#                                  access to the domain — see README "Network").
#                 readable       = plain fetch returns real, dated article text.
#                 thin           = fetch returns little/no article text (heavy JS).
#                 needs-browser  = requires a browser render (Playwright), not a
#                                  plain fetch. Routine 1 should use the browser path
#                                  or fall back to the aggregator for this platform.
#                 broken         = URL dead / blocked; do not use.
#   note        Free-text guidance for the routine.
#
# VALIDATION: done 2026-07-21 by in-session curl from an egress-enabled environment.
# IMPORTANT — fetch method: fetch pages with an in-session client (curl / requests /
# browser), NOT the managed WebFetch tool. With egress open, curl gets HTTP 200 from
# every domain, but WebFetch's fetcher is blocked by the sites' anti-bot (403) even
# then. Dates/titles are most reliably read from each article's JSON-LD
# ("datePublished") and og: meta tags. Re-validate on drift.

sources:
  # ── Google ──────────────────────────────────────────────────────────────────
  - key: google_ads_commerce_blog
    platform: google_ads
    title: Google Ads & Commerce Blog
    url: https://blog.google/products/ads-commerce/
    type: official
    validation: readable
    note: Primary Google Ads source. Server-rendered; article links + JSON-LD dates
      read fine via curl (verified article dated 2026-07-13).

  - key: google_search_blog
    platform: google_ads
    title: Google Blog — Search
    url: https://blog.google/products-and-platforms/products/search/
    type: official
    validation: thin
    note: Returns 200 but the article cards are JS-hydrated — no article links in the
      raw HTML. Deprioritize; Ads&Commerce blog + Search Engine Land cover Search-ads
      news. Promote to readable only if a browser-render path is added.

  - key: google_ads_announcements
    platform: google_ads
    title: Google Ads Help — New Features & Announcements
    url: https://support.google.com/google-ads/announcements/9048695?hl=en
    type: official
    validation: readable
    note: Official product announcements. Large server-rendered page, all entries
      inline (verified). High signal-to-noise for features.

  - key: searchengineland_google
    platform: google_ads
    title: Search Engine Land — Google
    url: https://searchengineland.com/library/platforms/google
    type: aggregator
    validation: readable
    note: ~50 article links with JSON-LD dates (verified article dated 2026-07-17).
      Overlaps official Google sources; prefer official as canonical on dedup. NB the
      top links are a site-wide recent/popular block — pick platform-specific items.

  # ── Meta ────────────────────────────────────────────────────────────────────
  - key: meta_business_news
    platform: meta
    title: Meta for Business News
    url: https://www.facebook.com/business/news
    type: official
    validation: needs-browser
    note: Plain fetch returns a Facebook "Error"/noindex page (HTTP 400), no content.
      Needs a logged-in browser render. Rely on meta_newsroom_tech + searchengineland_meta
      instead; revisit only if a browser+auth path is worth it.

  - key: meta_newsroom_tech
    platform: meta
    title: Meta Newsroom — Technologies
    url: https://about.fb.com/news/category/technologies/
    type: official
    validation: readable
    note: Server-rendered newsroom; ~14 article links + JSON-LD dates (verified article
      dated 2026-07-21). Filter HARD — much is non-ads (consumer/policy/PR).

  - key: searchengineland_meta
    platform: meta
    title: Search Engine Land — Meta
    url: https://searchengineland.com/library/platforms/meta
    type: aggregator
    validation: readable
    note: ~55 article links with dates (verified). Primary practical Meta-ads source
      since facebook.com/business/news is browser-gated.

  # ── TikTok ──────────────────────────────────────────────────────────────────
  - key: tiktok_business_announcements
    platform: tiktok
    title: TikTok for Business Blog — Announcements
    url: https://ads.tiktok.com/business/en-US/blog?category=Announcements
    type: official
    validation: needs-browser
    note: 200 but a JS shell — no post titles/dates in the raw HTML (~1.3MB, no <time>).
      Needs a browser render. Rely on searchengineland_tiktok until then.

  - key: searchengineland_tiktok
    platform: tiktok
    title: Search Engine Land — TikTok
    url: https://searchengineland.com/library/platforms/tiktok
    type: aggregator
    validation: readable
    note: ~57 article links with dates (verified). Primary practical TikTok-ads source
      since the official blog is browser-gated.

  # ── LinkedIn ────────────────────────────────────────────────────────────────
  - key: linkedin_marketing_blog
    platform: linkedin
    title: LinkedIn for Marketing Blog (LinkedIn Ads)
    url: https://www.linkedin.com/business/marketing/blog/linkedin-ads
    type: official
    validation: needs-browser
    note: Raw HTML exposes only nav/category links, not article headlines — the post
      list is JS-loaded. Needs a browser render. No strong LinkedIn-ads aggregator;
      flag gaps to a human until a browser path exists.

  # ── Bing / Microsoft Advertising ────────────────────────────────────────────
  - key: bing_blogs
    platform: bing
    title: Bing Blogs
    url: https://blogs.bing.com/search?page=1
    type: official
    validation: readable
    note: Server-rendered; article text + <time> tags present (verified). Broad
      Bing/search news — filter for Microsoft Advertising relevance.

  - key: searchengineland_microsoft
    platform: bing
    title: Search Engine Land — Microsoft Advertising
    url: https://searchengineland.com/library/platforms/microsoft/microsoft-advertising
    type: aggregator
    validation: readable
    note: ~62 article links with dates (verified). Often higher signal for Microsoft
      Ads than the broad Bing blog.
